# ml-sandbox
Load galaxy images and test ML classifiers/fitters on them, as practice for/benchmarking different methods. 

Files:
- `models.py`: various model classes for different tasks
- `dataset.py`: handles galaxy dataset loading for PyTorch 
- `train_eval.py`: handles model training
- `utils.py`: common util functions


